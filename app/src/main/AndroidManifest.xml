<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mamunsproject.sokol_simer_joruri_number">

    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:hardwareAccelerated="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sokol_Simer_Joruri_Number">
        <activity android:name=".WebView_"/>

        <activity android:name=".KoytiAccount" />
        <activity android:name=".Offers"/>
        <activity android:name=".TeletalkCode" />
        <activity android:name=".AirtelCode" />
        <activity android:name=".RobiCode" />
        <activity android:name=".GrameenCode" />
        <activity android:name=".AllSim" />
        <activity android:name=".BanglalinkCode" />
        <activity android:name=".MainActivity">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>